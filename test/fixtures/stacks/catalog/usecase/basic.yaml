components:
  terraform:
    aws-vanta/basic:
      metadata:
        component: target
      vars:
        enabled: true
        external_id: "test-external-id-12345"
        iam_role_name: "vanta-auditor-basic"
